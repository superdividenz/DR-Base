# PostgreSQL (required for doctor tracking)
DATABASE_URL=postgresql://user:password@localhost:5432/dr_tracking
# Railway: add PostgreSQL plugin, then copy DATABASE_URL from Variables

# Optional
PORT=3000
